<?xml version="1.0" encoding="UTF-8"?>
<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.victor.mapper.StudentMapper">
    <insert id="insert" parameterType="com.victor.pojo.Student">
        insert into student (name,no,gender,phone,degree,clazz_id,id_card,is_college,address,graduation_date)
        values (#{name}, #{no}, #{gender}, #{phone}, #{degree}, #{clazzId}, #{idCard}, #{isCollege}, #{address}, #{graduationDate})
    </insert>


    <update id="update">
        UPDATE student
        <set>
            <!-- 更新字段 -->
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="no != null and no != ''">
                no = #{no},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
            <if test="idCard != null and idCard != ''">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null">
                is_college = #{isCollege},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="degree != null">
                degree = #{degree},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
            <if test="violationCount != null">
                violation_count = #{violationCount},
            </if>
            <if test="violationScore != null">
                violation_score = #{violationScore},
            </if>
            <if test="clazzId != null">
                clazz_id = #{clazzId},
            </if>
            <!-- 最后一个字段不需要逗号 -->
            <if test="updateTime != null">
                update_time = #{updateTime}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <update id="updateViolation">
        update student
        set violation_score = violation_score + #{score},
            violation_count = violation_count + 1
        where id = #{id}
    </update>

    <delete id="deleteByIds">
        delete from student where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="getList" resultType="com.victor.pojo.Student">
        select * from student
    </select>


    <select id="getById" resultType="com.victor.pojo.Student">
        select * from student where id = #{id}
    </select>


    <select id="countStudentCountData" resultType="java.util.Map">
        select c.name as classList, count(s.id)
            as dataList from clazz c left join student s
                on c.id = s.clazz_id group by c.name order by count(s.id) desc
    </select>

    <select id="countStudentDegreeData" resultType="java.util.Map">
        select (case degree
            when 1 then '初中'
            when 2 then '高中'
            when 3 then '大专'
            when 4 then '本科'
            when 5 then '硕士'
            when 6 then '博士'
        end) as classList,count(*) as dataList from student
        group by degree
    </select>
</mapper>